# 开发日志 (Development Log)

## 2026-W21~W24 (5月20日 - 6月16日)

### 概要
- 完成了从基础类型到完整语言特性的全面扩展：字符串、浮点数、布尔字面量、负数、除法、比较运算符扩展（`<=`、`>=`、`!=`）。  
- 实现了函数定义与调用（支持参数、返回值、递归、嵌套调用）。  
- 增强了 REPL 体验：多行输入（行尾 `\` 续行）、`dump` 命令查看变量。  
- 完善了错误报告：解析和运行时错误均显示行列号。  
- 添加了 `print` 语句及字符串拼接（支持字符串与数字混合）。  
- 实现了基于 egui 的图形界面（IDE），支持代码编辑、运行、输出显示和变量监视。  
- 通过了覆盖所有特性的集成测试。

### 新增
- **值系统**：`Value::Float(f64)`、`Value::String(String)`，实现 `as_float()`、`as_string()` 和真值判断。
- **类型系统**：`Type::Float`、`Type::String`。
- **表达式**：`Expr::FloatLit`、`Expr::StringLit`、`Expr::BoolLit`、`Expr::Call`、`Expr::Neg`。
- **语句**：`Stmt::Print`、`Stmt::For`、`Stmt::FuncDef`、`Stmt::Return`。
- **运算符**：`/`、`<=`、`>=`、`!=` 以及一元负号 `-`。
- **语法解析**：支持浮点字面量、字符串字面量、布尔字面量；添加关键字 `float`、`string`、`true`、`false`、`print`、`for`、`to`、`func`、`return`；函数参数列表、返回类型语法。
- **求值器**：
  - 浮点数四则运算与比较，混合类型自动提升。
  - 字符串拼接（`+` 支持字符串与整数/浮点混合）。
  - 函数定义存储与调用（支持递归，返回值通过 `EvalError::Return` 传播）。
  - 错误信息附加行列号（基于源码字节偏移计算）。
- **REPL 增强**：
  - 多行输入：行尾 `\` 续行，自动拼接并保留换行。
  - 内置命令：`dump`（调用 `dump_env` 打印所有变量）。
  - 解析错误美化（显示行列、期望 token）。
- **图形界面**（`gui.rs`）：基于 `egui` 的简易 IDE（代码编辑器、运行按钮、输出区域、变量监视器），通过 `--gui` 参数启动。
- **环境调试**：`Environment::dump_to_string()` 用于 GUI 变量显示。

### 修改
- 重构 `Evaluator` 错误类型为 `EvalError`，区分 `Runtime` 和 `Return`。
- 修正 `grammar.lalrpop` 中分号处理：移除 `Return` 规则内的显式分号，由 `StmtList` 统一消费。
- 调整一元负号优先级，放入 `Factor` 规则，避免与减法冲突。
- 修正除法优先级，移至 `Term` 规则（与乘法同级）。
- 统一 `get_line_col` 函数，正确转换字节偏移为行列。
- 修复 `for` 循环中更新语句的副作用（使用子求值器执行）。
- 删除了 `Num` 规则，直接使用 `INT_NUM` 和 `FLOAT_NUM` token。
- 调整 GUI 布局，使输出区域固定高度、可拖拽，编辑器自动填满剩余空间。

### 计划
- 数组类型（`int[]`、索引访问、内置函数）。
- 模块/导入系统（`import "file.comix"`）。
- 内置函数库（`len`、`input`、类型转换等）。
- 字节码虚拟机优化。


## 2026-W9 (5月13日 - 5月19日)

### 本周概要
- 修改了部分代码以增强可读性，修复了若干偶发性解析与运行时错误（如关键字识别缺失、变量声明顺序问题等）。

### 新增
- 无。

### 修改
- 修正 `grammar.lalrpop` 中变量声明规则及关键字匹配（添加 `var` 关键字支持）。
- 优化 `environment.rs` 中错误信息的表述，使其更清晰。
- 调整 `evaluator.rs` 中部分类型转换的错误提示，增强用户友好性。
- 统一代码注释风格，移除冗余代码。

### 计划
- 进一步完善语法，支持 `for` 循环或 `print` 内置函数。
- 扩展功能：增加字符串类型、数组等。
- 优化交互体验，提供更详细的错误定位信息（行号、列号）。



## 2026-W8 (5月6日 - 5月12日)

### 本周概要
- 为 `evaluator.rs` 补充了单元测试，验证了核心执行逻辑的正确性。
- 编写了 REPL 入口 `main.rs`，实现了交互式命令行环境和脚本文件执行功能，集成了解析器与求值器。

### 新增
- `main.rs` – 程序入口，支持两种运行模式：
  - 无参数时启动 REPL（支持 `exit`、`clear` 命令）。
  - 带文件参数时读取脚本并执行。
- 单元测试（位于 `evaluator.rs` 中）– 对 `if`、`while`、变量作用域等场景进行验证。

### 修改
- 移除了原开发阶段用作程序入口的 `lib.rs`（其功能已合并至 `main.rs` 及模块声明中），保持项目结构清晰。

### 计划
- 进行端到端集成测试，验证解释器在多语句、嵌套作用域、错误处理等方面的健壮性。
- 考虑增加内置函数（如 `print`）以提升交互性。



## 2026-W7 (4月29日 - 5月5日)

### 本周概要
- 完成了求值器模块 `evaluator.rs` 的编写，实现了对 AST 的遍历执行，支持变量声明、赋值、控制流（`if`/`if-else`/`while`）及表达式求值，与值系统和环境模块完成对接。

### 新增
- `evaluator.rs` – 实现 `Evaluator` 结构体，核心功能包括：
  - `evaluate(&mut self, stmts: &[Stmt]) -> Result<Value, String>`：执行语句列表，表达式语句自动打印非空值。
  - `execute(&mut self, stmt: &Stmt) -> Result<Value, String>`：单条语句分发执行。
  - `eval_expr(&self, expr: &Expr) -> Result<Value, String>`：表达式递归求值，支持整数/布尔运算、比较、逻辑运算及变量读取。

### 修改
- 无（所有原有方法和签名保持不变，仅新增功能）。

### 计划
- 为 `evaluator.rs` 编写单元测试，覆盖变量作用域、控制流和错误处理。
- 编写 REPL 入口 `main.rs`，集成解析器与求值器，验证完整项目可行性。



## 2026-W6 (4月22日 - 4月28日)

### 本周概要
- 对 `environment.rs` 进行了功能性扩展，增加了调试辅助方法和局部查询能力，显著提升了开发和调试效率，为后续求值器的实现提供了更友好的环境操作接口。

### 新增
- **调试输出 `dump()`**：递归打印当前环境及所有父环境中定义的变量及其值，支持缩进显示层级关系。
- **局部查询 `get_local(name)`**：仅获取当前作用域的变量值，不沿父链向上查找，便于检查变量遮蔽情况。
- **局部存在判断 `defined_local(name)`**：快速判断变量是否在当前作用域直接定义（而非继承自父环境）。

### 修改
- 无（所有原有方法和签名保持不变，仅新增功能）。

### 计划
- 开始编写 `evaluator.rs`，利用增强后的环境执行 AST。
- 为 `evaluator` 编写集成测试，覆盖变量作用域、控制流和错误处理。
- 考虑增加内置函数（如 `print`）以提升交互性。



## 2026-W5 (4月14日 - 4月21日)

### 本周概要
- 利用 LALRPOP 工具，通过编写 `grammar.lalrpop` 文件，完成了 Comix 语言的语法定义和解析器生成。

### 新增
- `grammar.lalrpop` – 定义 Comix 语言的词法规则（关键字、运算符、标识符、数字等）和语法规则（语句、表达式、运算符优先级等），作为 LALRPOP 解析器生成器的输入。

### 修改
- 在 `Cargo.toml` 中增加了对 `lalrpop`（构建依赖）和 `lalrpop-util`（运行时依赖）的引用，并启用了 `lexer` feature 以使用内置词法分析器。
- 在 `lib.rs` 中添加了 `lalrpop_mod!(grammar)` 宏调用，用于引入生成的解析器模块。
- 修复了开发过程中遇到的编译错误及语法规则中的歧义问题。

### 计划
- 初步实现求值器（`evaluator.rs`），遍历 AST 并执行程序逻辑，与环境和值系统集成。



## 2026-W4 (4月1日 - 4月13日)

### 本周概要
- 完成了解释器底层核心模块的搭建，包括运行时值系统、抽象语法树定义和变量环境。所有模块已通过单元测试，为后续开发奠定基础

### 新增
`value.rs` – 定义运行时值枚举 Value，支持 Int(i64)、Bool(bool)、Null 三种类型
实现方法：is_truthy()（用于条件判断）、as_int()、as_bool()、is_null()，并实现 Display trait 用于打印

`ast.rs` – 定义抽象语法树节点：
- Expr 枚举：包括标识符、数字字面量、二元运算、逻辑非
- Stmt 枚举：包括变量声明、赋值、if/if-else、while、代码块、表达式语句
- Opcode 枚举：算术、比较、逻辑运算符
- Type 枚举：Int、Bool（用于变量声明的类型注解）

`environment.rs` – 实现变量环境：
- 使用 Rc + RefCell 管理嵌套作用域
- 提供 define()、get()、assign() 方法，支持变量定义、查找和修改

### 修改
- 修复了开发中存在的BUG

### 计划
- 编写手动构造 AST 的集成测试，验证求值逻辑与环境的交互
- 开始设计 grammar.lalrpop 的语法规则草稿
